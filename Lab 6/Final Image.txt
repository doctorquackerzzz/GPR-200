// GLSL STARTER CODE BY DANIEL S. BUCKSTEIN
//  -> IMAGE TAB (final)

// used https://www.shaderific.com/glsl-functions/ for help with the mix function
//------------------------------------------------------------
// SHADERTOY MAIN

// mainImage: process the current pixel (exactly one call per pixel)
//    fragColor: output final color for current pixel
//    fragCoord: input location of current pixel in image (in pixels)
void mainImage(out color4 fragColor, in sCoord fragCoord)
{
    vec4 screen = vec4(1.0, 1.0, 1.0, 1.0);
    // setup
    // test UV for input image
    sCoord uv = fragCoord / iChannelResolution[0].xy;
    
    // TESTING
    // set iChannel0 to 'Misc/Buffer A' and fetch sample
    
    vec4 original = texture(iChannel0, uv);
    vec4 gaussBlur = texture(iChannel1, uv);
    
    	//original add
    //fragColor = original + gaussBlur;
    
    	//mixes
    //fragColor += mix(original, gaussBlur, gaussBlur.a);
    fragColor += mix(gaussBlur, original, original.a);
    
    	//screen
    //fragColor = screen - (screen - original) * (screen - gaussBlur);
    
    	//multiplicative
    //fragColor = (original * gaussBlur) + 0.1;
    
    	//subtract
    //fragColor = (original-gaussBlur);
    
    //test output
   // fragColor = texture(iChannel1, uv);
}